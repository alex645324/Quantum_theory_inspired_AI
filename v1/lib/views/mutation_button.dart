// MutationButton view for QCI Tic-Tac-Toe
// Triggers rule changes on demand

import 'package:flutter/material.dart';

class MutationButton extends StatelessWidget {
  const MutationButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mutation Button - TODO: Implement'),
    );
  }
} 